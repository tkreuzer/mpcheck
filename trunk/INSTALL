You need to install:
	GMP 4.1.0
	MPFR 2.2.0 (Because version < 2.2.0 don't handle well overflow).

Once they are installed, use the classic way to install this program:

	./configure
	make
	make check
	make install

Use ./configure --help to get more options.
