2005-03-31  Patrick Pelissier  <pelissip@morpork>

	* INSTALL, config.guess, config.sub, install-sh, missing, mkinstalldirs, prepare:
	Remove files generated by autoreconf.
	Run autoreconf -i to generate them.

	* native.c: Add forgotten file.

	* mpcheck-double.c, mpcheck-float.c, mpcheck-longdouble.c:
	Add forgotten files.

	* INSTALL, Makefile.am, NEWS, TODO, VERSION, check.c, configure.in, func.c, mpcheck.h, rnd_mode.c, setup.c, test.c, ulp.c:
	New version 2.0.0.
	Rewrite many things.
        Complete new and more flexible architecture.
        Add real support for double, float and long double.
        Precision of native C types is detected at run time.
        Auto-detect the available functions at configure time.
        Check if Rounding mode if available at compile time
        (detecting if FE_TONEAREST is defined).
        Check if Rounding mode if available at execution time
        by detecting if we get at least one bit of precision
        for exp(5.0).
        Default is to check all C types (float, double and long double).
        Needs MPFR 2.2.0 (Previous release have known overflow problems).
        Auto-restriction to non-overflow interval when checking functions.

2005-03-23  Paul Zimmermann  <zimmerma@morpork>

	* TODO: added items suggested from Vincent

	* README, TODO, check.c, func.c, mpcheck.h, test.c, NEWS:
	added support for libmcr
	added option -dir (only rounding to nearest)
	cleanep options

2005-03-21  Paul Zimmermann  <zimmerma@morpork>

	* TODO: added new item

2005-03-19  Paul Zimmermann  <zimmerma@morpork>

	* check.c, configure.in, VERSION: fixed unique version number

2005-03-18  Paul Zimmermann  <zimmerma@morpork>

	* TODO, check.c: problem with sqrt/cbrt/hypot with prec=64
	added items in TODO

	* TODO: another suggestion from Vincent

	* setup.c: remarks from V. Lefe`vre

	* check.c, func.c, mpcheck.h, setup.c:
	fixed to work for "float" (24 bits)

2005-03-17  Patrick Pelissier  <pelissip@morpork>

        * Makefile.am, check.c, configure.in, func.c, mpcheck.h, rnd_mode.c, setup.c, test.c, ulp.c:
        Add GNU GPL notice in the files.
        Add 'make check' to Makefile.am

        * INSTALL: Improve Install Notes.

        * INSTALL, README, TODO, AUTHORS: Fill in the files.

2004-05-06  Patrick Pelissier  <pelissip@morpork>

        * ChangeLog, NEWS, check.c, configure.in, setup.c, test.c:
        Add the ability to select the precision.

2004-05-04  Patrick Pelissier  <pelissip@morpork>

	* Makefile.am, acinclude.m4, check.c, configure.in, test.c:
	Fix a bug in the configure (Use LIBS instead of LDADD).

2004-05-03  Patrick Pelissier  <pelissip@morpork>

	* setup.c: Fix a typo.

	* configure.in: Fix a tiny bug.

	* missing, mkinstalldirs, mpcheck.c, mpcheck.h, prepare, rnd_mode.c, setup.c, test.c, ulp.c, COPYING, ChangeLog, INSTALL, Makefile.am, NEWS, README, TODO, VERSION, acinclude.m4, check.c, config.guess, config.sub, configure.in, func.c, install-sh, makefile, AUTHORS:
	Update MPCHECK to be distributed.
	Run "prepare" script to prepare the installation.

2004-04-06  Paul Zimmermann  <zimmerma@morpork>

	* makefile, mpcheck.c: removed "local" directories from makefile
	removed MATHLIB define in mpcheck.c (default is to check libm)

2002-10-31  Paul Zimmermann  <zimmerma@morpork>

	* mpcheck.c: replaced ifdef's by macro SUFFIXL
	replaced XXX by correct values (hopefully)

2002-10-19  Paul Zimmermann  <zimmerma@morpork>

	* mpcheck.c:
	added options to disable monotonocity, symmetry, range check

2002-10-18  Paul Zimmermann  <zimmerma@morpork>

	* mpcheck.c: removed commented functions

	* mpcheck.c: print also values for maximal ulp difference

2002-10-18  nrevol  <nrevol@morpork>

	* mpcheck.c: Range incorrect for acosh, corrected.

	* mpcheck.c:
	test for range, monotonicity and symmetry added, 18-10-2002

2002-10-17  Paul Zimmermann  <zimmerma@morpork>

	* mpcheck.c: new version with support for long double

	* makefile, mpcheck.c: New file.

	* makefile, mpcheck.c: creation

